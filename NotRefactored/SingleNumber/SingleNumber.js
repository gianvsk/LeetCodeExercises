/* function singleNumber(nums) {
    nums.sort();
    nums = nums.filter(function (el, i, arr) { return arr.indexOf(el) === i && arr.lastIndexOf(el) === i; });
    return nums.length === 1 ? nums[0] : 0;
}
;
 */